# Raw generator output is a working file, not source — regenerate any time.
pending-recipes.json
# curated-recipes.batch*.json are intentionally NOT ignored: they record the
# human curation decisions that fed each promote, kept as provenance.
